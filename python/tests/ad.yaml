table1:
  database : "default"
  name : "hourly_metrics"
  ts_col : "event_ts"
  lookback_window : "84600"
  mode : "new"
  # include any grouping columns or metrics you wish to detect anomalies on
  partition_cols : ["winner"]
  metrics : ["advertiser_impressions"]
table2:
  database : "default"
  name : "monthly_metrics"
  mode : "new"
  ts_col : "event_ts"
  lookback_window : "84600"
  # include any grouping columns or metrics you wish to detect anomalies on
  partition_cols : ["winner"]
  metrics : ["advertiser_impressions", "clicks"]